# Bybit Scalper Terminal — Stage 5.17

Mobile-first Bybit Global scalping terminal.

Stage 5.17:
- mobile timeframe buttons moved onto the chart;
- B/S strength strip moved directly into the mobile trading dock;
- mobile fullscreen button and browser Fullscreen API support;
- installable PWA shell (`manifest.webmanifest` + service worker + icons);
- standalone app mode for Android home-screen installation;
- existing realtime market/trading/Multi-TP/AUTO S/R mechanics preserved;
- trading remains disabled until explicitly enabled.


Stage 5.17: mobile timeframe moved onto chart; mobile Volume/Orderbook toggles moved into chart toolbar; PWA manifest simplified for Android installability and service-worker cache version bumped.


Stage 5.17: mobile chart controls moved to a dedicated top row; live price/score strip is below it to prevent overlap. Controls are horizontally scrollable and compact for narrow Android screens.


Stage 5.17: mobile-first UI shell, chart controls on-chart, compact mobile header, bottom-sheet trade panel, PWA shell refinements.
